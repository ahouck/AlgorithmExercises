/**
 * Function question_wildcard_match(text, query) adds the quantifier metacharacter ? to the query character set. x? 
 * in query matches either zero occurrences or one occurrence of x in text, where x is a non-quantifier character.
 *
 */
 
 
 function question_wildcard_match(text, query) {
    
 {
